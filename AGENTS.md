# DesktopTodoDaemon 协作约定

- 默认用中文沟通与编写面向用户的文案。
- 产品目标是快速看见、添加和完成待办，并留下可直接阅读的本地 Markdown 记录；新增功能不得破坏这一主线。
- 涉及交互、数据可靠性、窗口行为、启动方式或架构选择时，主动检索并核验优质开源项目的真实实现与经验，再结合本项目约束决定是否采用。记录关键参考、采用内容与未采用原因，不能仅凭项目热度照搬。
- 优先原生 macOS 能力和无运行时依赖方案。数据始终保留在用户本机；引入联网、云同步或外部服务前需用户明确授权。
- 修改持久化格式时必须保留向后兼容或提供迁移，并增加往返读取测试，避免完成历史丢失。

